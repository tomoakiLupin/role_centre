我现在希望在创建一个定时任务
自动重写格式化 @data/role_assignments.json 的文件，将原来按照 role ID 分组在每天的 4 点自动重写格式化为按照 user ID 的分组

我认为还有我问题

```json
{
    服务器ID: {
        用户ID: [
            "角色ID","角色ID"
        ]
    }
}
```
这样会不会好一点？

---

现在使用 @doc/outinput/scanner.js 完全重构消息扫描器，要求使用这个文件的扫描逻辑，拼合现有表名还有 无效消息逻辑
完全重构当前数据库写入逻辑
无须向后兼容
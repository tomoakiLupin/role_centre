我现在希望完成一个 / 的命令，这个命令可以快速批量的分发身份组 

/批量分发 
- 身份组 ID（第一个）（必选）
- 身份组 ID（第二个）（可选）
- user ID str（一个长串的 userid 列表，使用 “，” 分割）
- 消息链接 （尝试通过消息链接解析获取 userid）
- 操作ID
- 有效期时间长度
- 是否跳过自动过期


你可以参照 @data/role_assignments.json 这个文件的数据结构

当输入操作 ID 时，是对于原有操作 ID 下的人员名单进行添加操作
输入时间长度就是修改 timeout 类型
忽略同理


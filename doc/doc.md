我现在希望完成一个 / 的命令，这个命令可以创建一个带按钮的面板，
在对应的频道发送 ED 嵌入式消息，通过这个面板连带的 按钮可以自选 加入/退出 指定的身份组

/创建自主获取面板
- 身份组ID
- 标题
- 内容
- image（URL）
- 频道（默认使用命令的频道）

注意：
消息要求自动置底，有新消息自动删除原来的消息发送新的，可以参见 @data/cache/role_distributors.json 的定位文件